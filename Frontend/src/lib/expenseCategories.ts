// Shared with the backend's EXPENSE_CATEGORIES (schemas/project_expenses.py,
// schemas/invoice_expenses.py) — keep both lists in sync.
export const EXPENSE_CATEGORIES = ['Airfare', 'Hotel', 'Parking / Transportation', 'Meals', 'Other'];
